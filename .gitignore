# Personal job-search data — keep local, never publish.
# These hold your live application funnel, dedup ledger, and private notes.
applications.json
seen-jobs.json
CLAUDE.local.md

# Local Claude Code settings (machine-specific, may hold permissions/paths)
.claude/settings.local.json
.claude/*.local.json

# OS / editor noise
.DS_Store
*.swp
